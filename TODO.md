# ScorbiumDashboard — TODO / Roadmap

## 🔴 Критично (Высокий приоритет)

### [ ] Исправление создания RBAC
- Проверить: создаётся ли админ при миграции `d5e6f7a8b9c0`
- Проверить: работает ли логин через DB (не только env fallback)
- Проверить: правильно ли работает `_require_permission` на всех маршрутах
- Проверить: `miniapp-login` — хардкод роли superadmin

### [ ] Исправление экспорта CSV файлов
- Проверить: `ExportService` — корректная выборка данных
- Проверить: `StreamingResponse` с правильными заголовками
- Проверить: CSV форматирование (разделители, кодировка UTF-8 BOM)
- Добавить: кнопки экспорта на страницы users, payments, subscriptions

### [ ] Оптимизация / Исправление недочётов фронтенда панели
- Убрать `alert()` в dashboard.html (заменить на console.log)
- Проверить: все скрипты и стили без ошибок
- Проверить: адаптивность на мобильных
- Убедиться: тема переключается корректно
- Проверить: графики Chart.js отображаются

---

## 🟡 Средний приоритет

### [ ] Показ истечения подписки / ключа в дашборде
- Добавить в dashboard route: запрос истекающих подписок (next 7 days)
- Добавить карточку/секцию "Истекают скоро"
- Отобразить: user_id, plan, expires_at, days_left

### [ ] Выдача ключей по дням, а не по тарифам
- Изменить логику `VpnKeyService.provision()` / API endpoint
- Вместо `plan_id` принимать `days: int`
- Рассчитать `expires_at = now + timedelta(days=days)`
- Обновить UI: форма с полем "дни" вместо выбора тарифа

### [ ] Создать CLI
- `python -m app.cli` или отдельный скрипт
- Команды:
  - `create-admin --username --password --role`
  - `reset-password --username`
  - `export-users --format csv|json`
  - `stats` — общая статистика
  - `backup` — дамп БД

---

## 🟢 Низкий приоритет

### [ ] Редизайн фронта Mini App
- Пересмотреть UI мини-приложения (`app/templates/miniapp/`)
- Унифицировать стили с основной панелью (Ocean Dark)
- Добавить анимации и glassmorphism

### [ ] Дополнительные улучшения
- Rate limiting на WebSocket endpoint
- Кэширование dashboard data (Redis)
- Логирование действий администраторов (audit log)
